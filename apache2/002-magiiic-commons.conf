<IfModule mod_dir.c>
  DirectoryIndex index.php index.html index.htm
  # DirectoryIndex index.html index.htm index.php /lib/wrap/wrap.php
  # DirectoryIndex index.html index.cgi index.pl index.php index.xhtml index.htm
</IfModule>

Alias /lib/ /opt/magic/lib/

RedirectMatch permanent ^/webmail(.*) http://mail.magiiic.media/
# Redirect /mail/config-v1.1.xml /cgi-bin/autoconfig.cgi

#  Alias /pipermail /var/lib/mailman/archives/public
#  RedirectMatch /cgi-bin/mailman/([^/\.]*)(.cgi)?(.*) https://DOMAIN:10000/virtualmin-mailman/unauthenticated/$1.cgi$3
#  RedirectMatch /mailman/([^/\.]*)(.cgi)?(.*) https://DOMAIN:10000/virtualmin-mailman/unauthenticated/$1.cgi$3

RewriteEngine on
#  RewriteCond %{HTTP_HOST} =webmail.DOMAIN
#  RewriteRule ^(.*) https://DOMAIN:20000/ [R]
#  RewriteCond %{HTTP_HOST} =admin.DOMAIN
#  RewriteRule ^(.*) https://DOMAIN:10000/ [R]
#  RewriteCond %{HTTP_HOST} !^DOMAIN$
#  RewriteRule ^(.*) http://DOMAIN$1          [R]

php_admin_value open_basedir "/opt/magic/lib:/usr/share:/var/lib:/var/tmp:."
#php_admin_value sendmail_path "/usr/sbin/sendmail -t -i -f webmaster@DOMAIN"
php_admin_value upload_max_filesize "100M"
php_value post_max_size "100M"

<IfModule mod_expires.c>
	ExpiresActive on

	ExpiresByType image/jpg "access plus 1 month"
	ExpiresByType image/jpeg "access plus 1 month"
	ExpiresByType image/gif "access plus 1 month"
	ExpiresByType image/png "access plus 1 month"
</IfModule>
