# Karen app defaults

# If we add other versions support, we will add verifications
PHPVER=8.3

[ -z "$init_base" ] && echo "$BASH_SOURCE[0] must only be sourced by karen helpers" && exit 1

# Protected configs
KAREN_BASE="${init_base:-$(dirname $(0))}"
KAREN_BIN="$KAREN_BASE/bin"
KAREN_DIR="$KAREN_BASE"
KAREN_HELPERS="$KAREN_BASE/app/helpers"
KAREN_PRESETS="$KAREN_BASE/presets"
KAREN_TEMPLATES="$KAREN_BASE/templates"

# User-defined configs (outside repository)
KAREN_ETC="${KAREN_ETC:-/etc/karen}"
