# Load average monit config

check system $HOST
   if loadavg (1min) per core > 4 for 5 cycles then alert
   if loadavg (5min) per core > 3 for 10 cycles then alert
   if cpu usage > 98% for 10 cycles then alert
   if memory usage > 98% then alert
#   if memory usage > 75% then alert
#   if swap usage > 25% then alert
